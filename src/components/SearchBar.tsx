import React, {useState} from 'react'

const searchBar = () => {
    return(
        <div>
            <div className="search">
                <span>TEXT</span>
                <button>Search</button>
            </div>
        </div>
    )
}

export default searchBar